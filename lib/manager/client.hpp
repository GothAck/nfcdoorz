#include <map>
#include <optional>
#include <string>

#include <capnp/ez-rpc.h>
#include <docopt/docopt.h>

#pragma once

namespace nfcdoorz::manager {
  // extern std::optional<capnp::EzRpcClient> client;

  // void init_client(std::map<std::string, docopt::value> &args);
}
