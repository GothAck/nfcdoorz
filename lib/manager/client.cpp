#include "lib/manager/client.hpp"

namespace nfcdoorz::manager {
  using namespace std;

// optional<capnp::EzRpcClient> client;

// void init_client(map<string, docopt::value> &args) {
// client.emplace(args["--ipc-fd"].asLong());
// }
}
